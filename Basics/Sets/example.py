nums = {1,2,"hello",4,5,1,1,1}
#sets does not allow duplicate values, it wil ignore those values.
print(nums)
print(len(nums))#in the above list we have added duplicate values as well (having total length as 8) but we will only get output as 5 becase 3 are duplicates.
print(nums)